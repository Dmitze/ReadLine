// Re-export the addBookScene from the refactored location
// This maintains backward compatibility while we complete the refactoring

export { default as addBookScene } from '../addBookScene';
export * from './utils';
